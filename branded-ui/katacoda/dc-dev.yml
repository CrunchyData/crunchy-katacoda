web:
  build: .
  environment:
    VIRTUAL_HOST: 'example-portal.katacoda.com'
    KATACODA_HOST: 'https://katacoda.com'
    KATACODA_USER: 'crunchydata'
    ENV: 'dev'
  ports:
    - 8080:3000
  restart: always
